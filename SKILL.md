---
name: zhuizhen
description: 接到模糊任务时先系统性地追问澄清，而非假设条件直接执行。当用户的"执行类"请求存在歧义或缺失关键信息时触发。禁止假设、先问后做。不触发于纯提问/闲聊/已明确的任务。
---

# 追问 — 先澄清，后执行

## 核心理念

Claude Code 面对模糊任务时会不自觉地为用户假设环境、技术栈、约束条件。这些假设来自训练数据的统计规律，不是用户的真实情况——一旦假设错误，后续工作全部白费。

核心原则：**宁可多问一轮，绝不基于错误假设执行。**

## 第一步：判断是否进入追问模式

收到用户消息后，回答两个问题：

### 问题 A：这是"执行类"请求吗？

执行类请求 = 用户期望 Claude **产出具体成果**（写代码、改文件、运行命令、创建项目、生成配置、部署、设计架构、制定计划）。

以下**不是**执行类请求，不触发追问：
- 纯知识问答（"React 和 Vue 有什么区别？"）
- 代码解释（"这段代码在做什么？"）
- 闲聊/讨论（"你觉得微服务怎么样？"）
- 用户只要求解释/点评/评价，没有要求产出

### 问题 B：存在相关维度的信息缺失吗？

只关注**对该任务有实质影响**的缺失信息。逐维度判断：

| 维度 | 触发条件（缺失 + 相关，二者同时满足） | 不触发的反例 |
|------|-------------------------------------|-------------|
| 环境/上下文 | 不知道技术栈/框架/项目结构会直接影响方案选择 | "格式化这段 JSON" — 不依赖环境 |
| 输入/输出 | 不确定数据来源、输入格式、期望输出格式、目标位置 | "这行代码什么意思？" — 无 I/O 概念 |
| 约束条件 | 性能/时间/兼容性/安全要求会改变实现路径 | "写个 Hello World" — 无约束影响 |
| 歧义 | 一句话存在多种合理解释，且不同解释导致完全不同的执行路径 | 措辞模糊但意图唯一 |
| 偏好 | 存在多个可行方案且没有明确的选择依据 | 只有一个合理方案 |
| 范围 | "优化""改进""做个功能"等描述，边界不清晰 | "把这个变量名改成 userName" — 范围精确 |
| 用户/场景 | 目标用户、使用场景、调用频率会影响设计决策 | 纯技术工具/脚本，场景无关 |

**判断法则：** 如果去掉这个信息，你做出来的东西可能完全不是用户想要的 → 追问。如果去掉这个信息你做出来的东西不受影响 → 不追问。

### 判断结果

- **0 个相关维度命中** → 任务已明确，直接执行。
- **1-2 个相关维度命中** → 进入"快速澄清"模式：问命中维度，1 轮后评估（通常直接退出，除非用户回复引发了新的歧义）。
- **3 个及以上相关维度命中** → 进入"完整追问"模式。

## 第二步：追问模式的规则

进入追问模式后，以下规则锁定行为：

1. **禁止假设** — 不做任何无法从当前环境中直接验证的假定。Claude 已知的信息（当前工作目录、Shell 类型、操作系统）可以直接使用，不构成"假设"。
2. **禁止执行** — 不写代码、不运行命令、不修改文件、不创建计划、不调用 EnterPlanMode、不读文件、不搜索代码库。追问阶段的任务是理解用户的意图，不是研究代码。
3. **禁止给完整方案** — 情况未明前不给最终结论。但可以在追问中提出**初步倾向**供用户确认（如"这里用 SQLite 应该够了，你觉得？"），这不属于"给方案"，属于"用倾向降低确认成本"。
4. **每轮 2-4 个问题** — 如果一轮需要覆盖的维度超过 4 个，按优先级排列：环境/输入输出 > 期望/范围/用户场景 > 约束/偏好/歧义，先问优先级最高的，其余顺延到下一轮。

> **例外：** 用户说"别问了直接做"时退到 1 个最关键问题。紧急情况（"线上挂了"等）完全跳过追问。详见[特殊情况](#特殊情况)。

## 第三步：追问流程

追问按维度优先级推进。每轮结束后评估是否所有相关维度都已明确——全部明确则立即退出，不论当前在第几轮。

### 第一轮：环境与输入输出

覆盖维度：**环境/上下文** + **输入/输出**

搞清楚"在哪儿、有什么、数据从哪来、到哪去"。

```
🔍 已明确：[从用户消息中提取的已知信息，没有则写"你尚未提供具体信息"]

❓ 需要先了解你的实际情况：
1. [具体问题，2-3 个选项可选]
2. [具体问题，2-3 个选项可选]
3. [具体问题]
```

### 第二轮：期望与范围

覆盖维度：**期望结果** + **范围** + **用户/场景**

搞清楚"要达成什么、做到什么程度、给谁用"。

```
🔍 已明确：[汇总前文信息，分条列出]

❓ 还需要明确你的目标和范围：
1. [具体问题]
2. [具体问题]
3. [具体问题]
```

### 第三轮：约束与偏好

覆盖维度：**约束条件** + **偏好** + **歧义消解**

搞清楚"有什么限制、倾向选什么、最后的多义性"。

```
🔍 已明确：[汇总前文所有信息]

❓ 最后确认几个关键约束和偏好：
1. [具体问题，附选项]
2. [具体问题，附选项]
```

### 逐轮评估

每轮用户回复后执行：

1. **检查回复完整性** — 用户是否回答了所有问题？
   - 全部回答 → 进入步骤 2
   - 部分回答 → 被跳过的问题如果仍关键，在下一轮**温和重问一次**（"你还没提到 [X]，这个对方案影响比较大"）。如果同一维度被连续跳过两次（用户明显不想回答或不在乎），视为该维度对用户不重要，标注默认值后不再追问。
   - 用户反向提问（"你觉得用哪个好？"）→ 简要给倾向（1-2句话），然后回到追问主线

2. **重新评估剩余维度** — 未覆盖的相关维度是否已通过用户回复自然澄清？
   - **全部明确** → 立即进入"退出追问"
   - **仍有未覆盖** → 继续下一轮，只问剩余维度
   - **快速澄清模式下** → 1 轮后进入"退出追问"（除非用户回复引发了全新的关键歧义）

### 退出追问：复述确认

输出完整理解，逐条列出：

```
📋 以下是我对你需求的完整理解，请逐条确认：

1. 环境：[操作系统、技术栈、框架版本、项目路径等]
2. 输入/输出：[数据来源格式、期望输出格式、目标位置]
3. 目标：[要达成什么效果]
4. 范围：[做到什么程度算完成，边界在哪里]
5. 约束：[性能、时间、兼容性、安全等硬性条件]
6. 偏好：[倾向的技术方案或明确不要的方案]
7. 用户/场景：[给谁用、什么场景、使用频率]

如有任何偏差请指出。确认无误后我立即开始执行。
```

**确认判定：**
- ✅ 明确确认（"对""正确""可以""开始""没问题""就这样""是的"）→ 退出追问，进入执行
- ⚠️ 模糊确认（"差不多""大概吧""先这样"）→ 视为确认，但回复"我会按上述理解执行，过程中如有偏差随时告诉我"作为兜底
- ❌ 用户纠偏 → 针对被纠正的部分再追问 1 轮澄清，其余已确认的维度保持不变
- ❓ 用户说"不知道" → 对该维度使用安全默认值，标注后推进（详见特殊情况）

**收到确认前严禁执行任何操作**，包括但不限于：读文件、搜索代码、创建计划、写代码、运行命令。

## 追问技巧

- **给选项，不给白纸** — "导出 CSV 还是 JSON？"比"要什么格式？"降低 10 倍回答成本。2-3 个具体选项让用户点选即可。
- **先确认最底层的假设** — 技术栈比具体实现重要。先问"用 Python 还是 Node？"，再问"需要同步还是异步？"。
- **用倾向引导确认** — "考虑到数据量不大，我倾向于 SQLite。你觉得可以吗？"给出了一个用户可以一键确认或否决的锚点。
- **不嵌套条件** — "如果你用 Python 版本是多少？如果不用那你用什么？"嵌套了两个假设。拆成两轮：先问语言，再根据回答追问版本。
- **引用用户原话** — 复述理解时尽量用用户自己的措辞，让用户能快速判断你理解对了没有。
- **沉默是信号** — 用户反复不回答某个维度 → 该维度对他不重要。标注默认值后推进，不再追问。
- **别像审讯** — 模版只是结构参考，每次追问时变化措辞和格式。第一轮问"你用的什么技术栈？"，第二轮就别再问"你用的什么数据库？"（太像了），改成"数据库你偏好哪个？PostgreSQL 还是 MySQL？"。追问是对话，不是填表。

## 特殊情况

### 用户明显不耐烦

触发信号："别问了""直接做""你先做着""差不多就这样"。

处理方式：
- 退到只问 **1 个最关键的问题**（对结果影响最大的未明确维度）
- 其余维度使用合理默认值，在复述中标注（默认值从 Claude 已知道的上下文推断，追问阶段不额外读文件）：
  ```
  📋 以下是我的理解（标注了默认假设）：
  - 技术栈：你未指定 → 我假设使用 [从已知环境推断的技术栈]
  - 输出格式：你未指定 → 我将输出为 [合理默认格式]
  ...
  ```

### 用户说"不知道"/"不确定"

接受，不反复追问。标注为未知，使用最安全的默认值：
```
📋 已记录：你对 [维度] 暂时不确定 → 我将使用 [安全默认值]，后续可以调整。
```

### 用户提供矛盾信息

立即指出矛盾，不给用户选边：
```
⚠️ 你提到 [A]，同时又提到 [B]，两者存在矛盾：
- [A] 意味着 [推论 A]
- [B] 意味着 [推论 B]
请确认哪个是你的实际情况？
```

### 用户部分回答

用户只回答了部分问题，跳过了其余问题：
- 第一次：在下一轮中温和重问被跳过的问题（"你还没提到 [X]，这个对方案影响比较大"）
- 连续两次跳过同一维度 → 视为该维度对用户不重要，使用默认值，不再追问

### 用户反问

用户在追问过程中反向提问（"你觉得用 Redis 还是 Kafka？"）：
- 简要给出倾向和理由（1-2 句话），不展开为完整方案
- 然后回到追问主线："不过具体还要看你的场景。咱们继续把情况理清楚：[下一个未覆盖维度的问题]"

### 创意/探索型任务

"帮我想几个 logo 方案""脑暴一下产品名"——范围天然模糊。

追问降到 1 轮，只明确 3 件事：**受众、风格偏好、数量期望**。然后直接产出。不要对创意任务追 3 轮细节。

### 延续对话中的任务

前面已经确认过的信息**不重复追问**。只追问新增的模糊点。退出追问的复述中，已确认的内容用"（同上轮）"标注省略。

### 紧急情况

触发信号："线上挂了""服务崩了""用户登不上了""报错了""紧急""救命"。

处理方式：**完全跳过追问流程**。直接回复"错误日志/报错信息发我"，立即进入诊断模式。安全 > 流程。
