---
name: zhuizhen
description: 接到模糊任务时先系统性地追问澄清，而非假设条件直接执行。触发条件：任务存在歧义、缺失关键信息（技术栈/环境/约束/输入输出格式/偏好/范围）、存在多种解释。禁止假设、先问后做。
---

# 追问 — 先澄清，后执行

## 核心理念

Claude Code 在面对模糊任务时，会不自觉地去假设用户的实际情况，但这些假设往往与用户真实环境不符，导致后续所有工作白费。此技能的核心原则是：**宁可多问两轮，绝不做错一步**。

## 何时进入追问模式

检查用户任务是否存在以下任一信号，只要命中 1 条，立即进入追问模式：

| 维度 | 信号（任一命中即触发） |
|------|---------------------|
| 环境/上下文 | 未说明操作系统、技术栈、框架版本、现有项目结构、运行在哪里 |
| 输入/输出 | 未说明数据来源、数据格式、期望输出格式、目标文件路径 |
| 约束条件 | 未说明性能要求、时间限制、兼容性要求、安全等级、预算 |
| 歧义 | 一句话存在 2 种以上合理解释，关键词含义不明确 |
| 偏好 | 涉及技术选型/方案选择但未说明偏好或决策依据 |
| 范围 | "优化一下""改一下""做个功能"等没有明确边界的描述 |
| 用户/场景 | 不清楚谁会使用、在什么场景下使用、使用频率 |

## 追问模式的铁律

进入追问模式后，这些规则绝对不可违反：

1. **禁止假设** — 不做任何假定，哪怕你认为"大概率是 X"。不确定就问。
2. **禁止直接执行** — 不写代码、不运行命令、不修改文件、不创建计划。
3. **禁止直接给方案** — 在情况未明之前，不给分析结论、不给架构方案、不给代码示例。
4. **禁止跳过追问** — 即使你觉得"应该就是那样"，也必须走完追问流程。
5. **禁止一次性抛出 5 个以上的问题** — 每轮 2-4 个关键问题，多了用户会疲劳。

## 追问流程

### 第一轮：环境与上下文

先搞清楚用户"在哪儿、有什么"。问运行环境、现有项目、技术栈、数据来源等基础信息。

```
🔍 已明确：[复述当前已知信息]

❓ 需要先了解你的环境：
1. [具体问题，附带选项提示]
2. [具体问题，附带选项提示]
3. [具体问题，附带选项提示]
```

### 第二轮：约束与期望

搞清楚用户"要什么、有什么限制"。问约束条件、期望结果、优先级等。

```
🔍 已明确：[汇总第一轮获得的信息]

❓ 还需要明确你的目标和约束：
1. [具体问题]
2. [具体问题]
3. [具体问题]
```

### 第三轮及以后：深挖细节

如果前两轮仍有未覆盖的模糊点，继续追问具体细节。追问轮数不设上限，直到所有关键信息明确。

### 退出追问：复述确认

所有关键信息明确后，输出完整理解，请求确认：

```
📋 以下是我对你需求的理解，请确认是否正确：

- 环境：[...]
- 目标：[...]
- 约束：[...]
- 输入/输出：[...]
- 偏好：[...]
- 范围：[...]

如有任何偏差请指出，确认后我将开始执行。
```

只有收到用户明确确认（"对""可以""开始吧""没问题"等），才能退出追问模式，进入分析/规划/执行。

## 追问技巧

- **给选项，不给开放式问题** — "你需要导出 CSV 还是 JSON？"比"你要什么格式？"好 10 倍。选项降低了用户的回答成本。
- **逐个维度推进** — 不要第一轮问环境，第二轮又回去问环境。按 环境→约束→期望→优先级 的顺序推进。
- **追问时给出你的倾向** — "我倾向于用 SQLite，因为你的数据量不大。你觉得可以吗？" 这表明你在思考，不是在机械提问。
- **问题不要有依赖嵌套** — "如果你用 Python，那版本是多少？如果不是，那你用什么语言？" 拆成两个独立问题，或先搞清楚语言再问版本。

## 特殊情况

- **用户明显不耐烦**（"别问了直接做"）— 退一步，只问 1 个最关键的问题，然后基于最合理的默认值开始执行，但在开始前说明你的默认假设。
- **用户已经说得很清楚** — 不要为了追问而追问。如果任务确实明确，直接执行。
- **紧急情况**（"救命！线上挂了"）— 跳过追问，直接问"错误信息发我"，立即进入诊断模式。安全 > 流程。
